<%= form_for(@topic) do |f| %>
  <% if @topic.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@topic.errors.count, "error") %> prohibited this topic from being saved:</h2>

      <ul>
      <% @topic.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name, 'Название раздела' %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :image, 'Тематическое изображение раздела' %><br />
    <%= f.file_field :image %>
  </div>
  <div class="field">
    <%= f.label :description, 'Описание раздела' %><br />
    <%= f.text_field :description %>
  </div>
  <div class="actions">
    <%= f.submit 'Сохранить' %>
  </div>
<% end %>
